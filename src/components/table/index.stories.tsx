import React from 'react';
import { Meta } from '@storybook/react';
import { Table } from '.';

const meta: Meta = {
  title: 'Components/Table',
};

export const Docs = () => {
  return (
    <>
      <Table></Table>
    </>
  );
};

export default meta;
